package aegis:host/log@0.1.0;

interface log {
    enum level {
        debug,
        info,
        warn,
        error,
    }

    /// Host enforces rate + size caps; no secrets in message.
    emit: func(level: level, message: string) -> result<tuple<>, string>;
}
