The canonical source is [`spec/SPEC.md`](https://github.com/botzrDev/aegis/blob/main/spec/SPEC.md)
in the repository. Status: **v0.1** (document version); the wire
`schema_version` it specifies is **2** — a different number, see §12. The record
file's extension is **`.aarl`** (ADR-0014), so examples write `session.aarl`.

Where this document and the code disagree, the code is the defect report.

{{#include ../spec/SPEC.md}}
