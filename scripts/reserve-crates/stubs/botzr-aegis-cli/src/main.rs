//! Placeholder binary — name reserved for the Aegis project.

#![forbid(unsafe_code)]

fn main() {
    eprintln!(
        "aegis CLI is not yet implemented. See https://github.com/botzrDev/aegis"
    );
    std::process::exit(1);
}
